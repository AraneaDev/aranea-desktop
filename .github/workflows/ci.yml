name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read

jobs:
  validate:
    name: validate
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install validation tools
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends --yes shellcheck libxml2-utils imagemagick ripgrep qt6-declarative-dev qt6-declarative-dev-tools

      - name: Validate shell syntax
        run: |
          find scripts hooks tests tools .githooks -type f -print0 |
            xargs -0 grep -lI '^#!.*sh' |
            xargs -r -n1 bash -n

      - name: Validate QML types
        run: bash tests/qml-types.test.sh

      - name: Run contract tests
        run: ./tests/run

      - name: Exercise installer profiles
        env:
          OMARCHY_INSTALLER_TEST: '1'
        run: |
          for profile in minimal full no_apps; do
            PATH="$GITHUB_WORKSPACE/tests/fake-bin:$PATH" \
              scripts/install.sh --dry-run --yes --profile "$profile"
          done

      - name: Validate SVG artwork
        run: |
          find integrations -type f -name '*.svg' -print0 | xargs -0 -n1 xmllint --noout

      - name: Validate raster artwork
        run: |
          find backgrounds screenshots -type f \( -name '*.png' -o -name '*.jpg' \) -print0 | xargs -0 -n1 identify

      - name: Run ShellCheck
        run: |
          find scripts hooks tests tools .githooks -type f -print0 |
            xargs -0 grep -lI '^#!.*sh' |
            xargs -r shellcheck -x

      - name: Upload diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: validation-diagnostics
          if-no-files-found: ignore
          path: |
            tests/.*-error
            /tmp/aranea-*.log
