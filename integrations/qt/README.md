# Aranea Qt integration

Use the `Aranea` directory with Kvantum when it is installed. If Kvantum is
absent, apply the values in `Aranea.kvconfig` through the host's native Qt
palette settings and keep `QT_STYLE_OVERRIDE` unset.

The integration is optional and does not replace a user's existing Qt theme
without an ownership backup.
