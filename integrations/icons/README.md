# Aranea icon integration

Aranea does not replace a complete system icon collection. The supported path
is to install only explicit Aranea folder/status overrides through the managed
integration linker; otherwise the active host icon theme remains in place.

This avoids breaking applications that rely on icon names outside the theme.
