# Aranea media integration

Media controls are optional and should use the host's native PipeWire/Pulse
theme interface. The palette uses mint for playback/healthy state, violet for
focus, gold for attention, and red for errors. If no native interface exists,
leave the media application unchanged rather than adding a persistent daemon.
