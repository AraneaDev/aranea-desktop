# Aranea media integration

Media controls are optional and should use the host's native PipeWire/Pulse
theme interface. When pavucontrol or pwvucontrol is installed, the managed
GTK fragment is placed at `~/.config/aranea/media/pavucontrol.css`; otherwise
the integration is skipped. No persistent daemon is added.
